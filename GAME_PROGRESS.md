# 호현지방 개선 기록

## 2026-09-23 — 햇살방과 캐릭터 색상

### 이번 변경

- 기존 2D 캐릭터가 kArchive 소파·튤립을 배치한 3D 방에서 걷습니다. 바닥 클릭·터치, 방향키/WASD, 터치 방향 버튼을 지원합니다.
- 벽·가구 충돌과 우회 경로를 적용하고, 화면을 나갈 때 WebGL 자원을 정리합니다. WebGL을 사용할 수 없으면 기존 꾸미기로 이동할 수 있습니다.
- 기존 28종 소품 편집과 저장은 유지합니다. 산책방은 별도 가구 배치이며, 벽·바닥색과 캐릭터 코디를 공유합니다.
- 도원의 중국풍 의상은 단발·만두머리를 모두 지원합니다. 힉스필드로 생성한 에셋을 로컬 배포에 포함했습니다.
- 머리·피부 색상 선택기, HEX, RGB 입력과 개별 초기화를 추가했습니다. 계정 저장·코디 보관함·PNG에도 반영됩니다.
- 저장 API는 새 색상과 의상 값을 검증합니다. 기존 계정·암호·지갑과 이전 섬/극장 배포 파일은 변경하지 않았습니다.

### 검증 기록

- 타입 검사와 규칙·저장·경로·색상 회귀 검사 159개 통과.
- 로컬 합성 계정으로 옷 선택, RGB/HEX 입력, 잘못된 값 차단, 코디 저장·복원, 재접속 유지 확인.
- 데스크톱의 실제 GLB·텍스처 로딩, 키 이동, 테이블 우회, 침대 충돌, 꾸미기 전환과 재진입 확인.
- 모바일 바닥 터치, 방향 버튼, 동작 줄이기, 세로·가로 화면과 가로 넘침 확인.
- WebGL 미지원 환경에서 2D 꾸미기로 이동하는 흐름 확인.
- Pages 패키지는 이미지 119개·GLB 2개를 콘텐츠 해시 URL로 배포합니다. 모델 출처는 화면, ASSETS.md, 배포물에 포함합니다.
- 실제 계정의 로그인·게임·지갑 변경을 사용하는 테스트는 하지 않습니다. 브라우저 검증은 외부 요청을 차단한 로컬 합성 계정으로 수행했습니다.

### 다음 점검 우선순위

1. 최신 사용자 요청과 작업 중인 변경을 먼저 확인합니다. 진행 중인 작업을 중복 실행하지 않습니다.
2. 공개 배포의 로딩·에셋 누락, 로그인·저장 오류를 우선 점검합니다. 실사용 계정이나 화폐를 임의로 변경하지 않습니다.
3. 모바일 성능을 측정하고, 필요하다면 방 진입 시 3D 코드·이미지를 읽도록 로딩 범위를 줄입니다.
4. 3D 방에 가구 이동·회전·저장을 확장할 때는 기존 2D 배치를 보존하고 새 저장 스키마와 충돌 경로를 함께 설계합니다.
5. 초대·재접속·미니게임 진행을 합성 참가자 환경으로 점검하고, 발견한 문제 하나씩 수정·검증·배포합니다.

2시간 주기 자동 점검은 이 작업에 연결되어 있습니다. 의미 있는 개선·오류·필요한 사용자 입력이 있을 때 알리고, 변화가 없으면 조용히 유지합니다. 자동 실행만을 근거로 추가 유료 생성이나 요금제 변경을 하지 않습니다.

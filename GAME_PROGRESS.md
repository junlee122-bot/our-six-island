# 범타듀 밸리 · 현재 상태와 다음 할 일

> 한 페이지 요약입니다. 2026-09-24까지의 상세 작업·배포 검증 일지는 [GAME_PROGRESS-archive.md](GAME_PROGRESS-archive.md)에 보관했습니다. 배포 기록은 이 파일에 쌓지 말고 GitHub Actions 실행 기록이나 Releases에 남깁니다.

## 현재 상태 (2026-09-24)

**게임**
- 로그인 → 3D 마을(80×60)에서 시작. 회관(고스톱·섯다), 별빛 카지노(홀덤·블랙잭·체스), 분장실(코디), 내 집(3D 산책방 + 2D 꾸미기), 친구 일곱 집.
- 다섯 미니게임은 서버 판정·비공개 패·범 예약/정산을 갖췄고, 같은 친구들과 다음 판을 이어 할 수 있습니다.
- 캐릭터는 2D 스프라이트(7인 걷기·달리기 모션), 배경·가구는 Three.js + kArchive/3DAssets GLB.

**서버** — Supabase Auth·Postgres·Edge Functions(`hohyeon-auth`, `hohyeon-api`). 계정 7개 고정.

**빌드·배포** — `npm run build:pages`가 `docs/`에 `index.html` + 해시 에셋을 만들고, main의 `docs/`를 GitHub Pages가 게시. CI(`.github/workflows/ci.yml`)가 타입·테스트·lint·빌드를 검사. Actions 배포(`pages.yml`)는 준비만 되어 있음(README "배포" 참고).

## 진행 중 (2026-09-24 점검 후속)

점검 보고서의 우선순위에 따라 다음을 병행해 고치는 중입니다.

- 보안: 활성화 코드 재발급·만료, 로그인 실패 잠금, 세션 만료, 저장 실패 시 덮어쓰기 방지, Realtime 채널 비공개화 (서버 배포 필요)
- 규칙·경제: 차례 제한시간과 자동 행동, 준비확인 제한시간, 판 도중 이탈 처리, 자리·선 회전, 출석 범·파산 구제
- 마을·3D: 위치 구분(마을/회관/카지노/분장실/내 방), 벽·건물 가림, 아바타 끼임
- 로비 UI: 접속 표시, 초대 알림·카운트다운, 방장 표시, 오류 토스트, 명칭·조사 통일
- 빌드·에셋: 코드 분할 허용, 이미지·GLB 경량화, 에셋 예산 검사, OG·아이콘·manifest, 미사용 의존성 정리, CI

## 다음 할 일

1. **보안 조치 마무리** — 서버 마이그레이션 → Edge Functions → Pages 순서로 배포하고, 사용하지 않은 활성화 코드를 모두 재발급 (ACCOUNTS.md).
2. **첫 로딩 줄이기** — 마을·방 3D, 게임 테이블, 옷장을 지연 로딩으로 분리한 효과를 모바일에서 측정. 로그인 화면의 장식용 GLB도 폼 표시 뒤로 미루기.
3. **Actions 배포로 전환** — `pages.yml`로 전환하고 `docs/`를 `.gitignore`에 넣어 저장소 비대화를 멈추기.
4. **"범타듀의 하루" 루프** — 출석 범 → 텃밭·과일 따기 → 상점(옷·소품 해금). 전제: 원장 지급/소비, 저장 스키마 마이그레이션.
5. **친구 집 방문·방명록**, 머리 위 말풍선, 접속 표시.
6. **사운드** — CC0 BGM 1 + 환경음 2 + UI음 5부터, 출처는 ASSETS.md에.
7. **정리** — 섬·극장 소스를 태그/브랜치로 옮기기, 예전 P2P 코드와 검증 스크립트 정리. (쓰지 않던 `components/ui`, `hooks/`, `lib/`, `components.json`은 삭제 완료.)

## 작업 규칙

- 실제 친구 계정·지갑·저장을 테스트에 쓰지 않습니다. 브라우저 검사는 외부 요청을 막은 합성 계정으로 합니다.
- 비밀 값(활성화·복구 코드, 비밀번호, 서비스 키)은 어디에도 적지 않습니다.
- 유료 이미지 생성은 구조 작업(지급/소비·저장 마이그레이션·area 일반화) 뒤에 계획적으로 합니다.
- 배포 전 `npm run check`와 `npm run build:pages -- --out <임시 폴더>` 결과를 로컬에서 열어 확인합니다.
